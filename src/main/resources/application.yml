server:
  port: 9000

spring:
  profiles:
    active: @profiles.active@
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver


---
spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://localhost:3306/knowing?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
logging:
  file:
    path: /log/knowing
  level:
    plus.knowing: WARN

---
spring:
  profiles: prod
  datasource:
    url: jdbc:mysql://172.16.31.101:3306/knowing?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
logging:
  file:
    path: /var/knowing/log
  level:
    plus.knowing: INFO
